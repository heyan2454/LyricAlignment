# 置信度门控：模型能不能自己说出哪里不可信（生成，勿手改）

> 由 `scripts/evaluation/report_confidence_gating.py` 从两份 JSON 生成；两份输入都来自**已付费的既有产物**，本实验没有跑任何新推理。

## 1. 域内（M4Singer 长音符富集样本，逐字符带真值）

- 18803 次测量，超 0.2 s 容差的错误率 **1.55%**；
- 各信号对「这个字符会超差」的判别力（rank-AUC，0.5 = 无信息）：

| 信号 | 需要真值？ | AUC |
|---|---|---|
| label_rank | **是（oracle）** | 0.9786 |
| neg_top5_mass | 否 | 0.9452 |
| entropy_nats | 否 | 0.9336 |
| neg_p_top1 | 否 | 0.8963 |
| duration | 否 | 0.6835 |

- 弃权（转人工复核）曲线，同样数据内拟合：

| 信号 | 复核比例 | 消除的错误 | 复核后剩余错误率 |
|---|---|---|---|
| entropy_nats | 1pct | 32.65% | 1.05%（原 1.55%）|
| entropy_nats | 2pct | 46.05% | 0.85%（原 1.55%）|
| entropy_nats | 5pct | 67.70% | 0.53%（原 1.55%）|
| entropy_nats | 10pct | 84.88% | 0.26%（原 1.55%）|
| entropy_nats | 20pct | 91.75% | 0.16%（原 1.55%）|
| label_rank | 1pct | 53.61% | 0.73%（原 1.55%）|
| label_rank | 2pct | 72.16% | 0.44%（原 1.55%）|
| label_rank | 5pct | 87.97% | 0.20%（原 1.55%）|
| label_rank | 10pct | 92.10% | 0.14%（原 1.55%）|
| label_rank | 20pct | 96.91% | 0.06%（原 1.55%）|

- 长字符与短字符同样可预测：short AUC(熵) 0.9363、long AUC(熵) 0.8869；但长字符错误率是 3.87% vs 短字符 1.11%。

## 2. 真歌批（无真值，目标是产品可见的缺陷：零长度区间）

- 13735 字，零长度率 **10.80%**（模型 raw 解码自身造成）；
- 每个信号在**同一批**上的 AUC：

| 信号 | AUC（越高越能预测塌陷） |
|---|---|
| end_entropy | 0.9227 |
| start_entropy | 0.9181 |
| neg_end_margin | 0.8884 |
| neg_start_margin | 0.8827 |

### 2b. 按歌留一交叉验证（阈值只用其它歌确定，才算得数）

| 信号 | 复核预算 | 消除的缺陷 | 复核后剩余缺陷率 |
|---|---|---|---|
| start_entropy | 5pct | 31.36% | 7.87%（原 10.80%）|
| start_entropy | 10pct | 53.67% | 5.60%（原 10.80%）|
| start_entropy | 20pct | 81.66% | 2.49%（原 10.80%）|
| end_entropy | 5pct | 33.65% | 7.60%（原 10.80%）|
| end_entropy | 10pct | 55.09% | 5.43%（原 10.80%）|
| end_entropy | 20pct | 82.27% | 2.40%（原 10.80%）|
| neg_start_margin | 5pct | 18.41% | 9.31%（原 10.80%）|
| neg_start_margin | 10pct | 42.82% | 6.90%（原 10.80%）|
| neg_start_margin | 20pct | 75.72% | 3.29%（原 10.80%）|

## 3. 结论

1. **模型自己能说出哪里不可信**：不需要真值的信号（softmax 熵 / top-1 边际）在域内对「超差」的 AUC 达 0.9336，在真歌上对「塌陷」的 AUC 达 0.9227；
2. **跨歌泛化成立**：按歌留一后，用 `end_entropy` 复核 10% 的字可消除 55.09% 的缺陷（剩余 5.43%），复核 20% 消除 82.27%（剩余 2.40%）；同样预算下同样本内拟合几乎没有更便宜，说明这不是阈值过拟合；
3. **label_rank 是最强信号但只能当 oracle**：它需要标注，因此适合用于**训练数据质检**（挑出模型与弱标注强烈冲突的条目复核），不能用于线上；
4. 落地成本极低：批产物里**已经存了**每字的 `raw_start_entropy`/`raw_end_entropy`，所以门控只是「取一个分位阈值 + 打标」，不需要额外前向。

## 4. 建议的下一步（可证伪）

- 在 demo 批量输出里加 `risk_score` 与 `review_flag`（按训练集外分位阈值），验证：被标记的字是否确实集中在长音型歌曲（与 r=+0.82 的时长因子一致）；
- 把复核预算固定为 10%，度量「人工只改被标记的字」相对全量复核的工作量下降；
- 若域外（无真值）AUC 明显低于域内，则改用**歌级**聚合风险（整段重跑或换解码）而不是字级。
