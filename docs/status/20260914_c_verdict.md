# C 臂判决（生成，勿手改；数字全部来自 JSON）

> C = 只给**长字符的结束点槽位**加权 ×3，**不复制任何条目** ⇒ 每步看到的唯一条目数与 A 相同；
> 设计、主指标与启动条件见 `20260914_c_arm_prereg.md`；本文件由 `python` 从
> `20260914_mech_ab_C_vs_A` / `20260914_mech_C_vs_start` / `20260914_warmstart_control/terminal_validation_ABC.json` /
> `20260914_matched_steps/C_vs_A.json` 生成。

## 三个权威读数

| 比较 | offset_long 均值Δ | 中位Δ | 截尾均值 | McNemar 好/差 | 均值 p(×4) | 二项 p(×4) | 判定 |
|---|---|---|---|---|---|---|---|
| C vs A（对照） | -14.57 ms | +0.0 | -1.15 | 11/3 | 0.011 (0.046) | 0.057 (0.230) | **迹象（未过校正）** |
| C vs 起点（是否真修好） | -5.02 ms | +0.0 | -1.24 | 6/3 | 0.192 (0.768) | 0.508 (1.000) | **无证据** |

- 短条目代价：同步数屏幕 L1 fixed **-0.29 pp**（0/6 个点更好，SE 0.092）；终态验证（全量 1,711 项）C 相对 A **+2.51 ms**（B 为 +1.29 ms），joint@80 -0.03 pp；

## 结论（按预注册规则读，不临场解释）

1. **C 相对起点没有买到长音改善**：offset_long Δ 只有 -5.02 ms（p=0.19，二项 p=0.51 ⇒ 无证据）。
2. 它相对 A 的 -14.57 ms 差值，**全部来自 A 自身在长音上退步**（A vs 起点在 §3g-4/§7 已登记为退化迹象），而不是 C 变好；
3. 代价是真的：短条目屏幕 -0.29 pp、终态 MAE +2.51 ms，且**比带覆盖混杂的 B（+1.29 ms）更大**；
4. ⇒ **C 没有 B 那个可以甩锅的副作用**（不复制条目 ⇒ 每步唯一条目数与 A 相同），所以这次不能再把"没收益"归因于工具；结论是：
   **在这个架构上，无论改采样（B）还是改目标权重（C），把监督压力向长音倾斜都换不来长音精度，却要付出短条目代价 ⇒ 数据/目标层面的再平衡已经走到边界。**
5. 按 C 预注册 §3 的判定（Δ≤−10 ms 且两检验 p<0.05 才算成立）⇒ **H-C 不成立**，该线收口；下一步只剩结构改动（时间戳头改为"起始 + 时长"参数化，或给结束点单独的解码/回归分支），那需要独立立项而不是今晚再加一个臂。

## 与前两条臂的一致性（同一形状的两次独立复现）

| 时长桶 | n | A 的结束点缺陷 | B 比 A 新修好 | B 比 A 新弄坏 |
|---|---|---|---|---|
| 0-0.5s | 4260 | 40 | 0 | 4 |
| 0.5-1s | 1215 | 37 | 4 | 4 |
| 1-1.5s | 596 | 30 | 4 | 3 |
| 1.5-2s | 217 | 11 | 4 | 0 |
| 2s+ | 127 | 11 | 5 | 0 |

⇒ B 的"长音段修好、最短段弄坏"形状在 C 上以另一种机制重现（C 的 offset_short 也是 +0.92 ms 偏坏、offset_long 偏好），**两次独立干预给出同一形状**是今晚最可信的机制信号；但两者对起点的净增益都不成立 ⇒ 形状真、幅度小、代价真。

## 补：C 的分段计数与 §5 形状断言的核对（10:14）

| 时长桶 | n | 起点 | A | C | C vs A 修好/弄坏 | B vs A 修好/弄坏 |
|---|---|---|---|---|---|---|
| 0-0.5s | 4260 | 41 | 40 | **42** | 0 / 2 | 0 / 4 |
| 0.5-1s | 1215 | 35 | 37 | **36** | 4 / 3 | 4 / 4 |
| 1-1.5s | 596 | 29 | 30 | **28** | 4 / 2 | 4 / 3 |
| 1.5-2s | 217 | 10 | 11 | **9** | 3 / 1 | 4 / 0 |
| 2s+ | 127 | 8 | 11 | **7** | 4 / 0 | 5 / 0 |

- 结束点缺陷总数：**起点 123、A 129、C 122**（同一批 6,415 个留出字符）；

### 三条读数合起来说什么
1. **§5 的形状预测部分证实**：不复制条目后，最短段的新弄坏从 B 的 **4** 降到 C 的 **2**，而 ≥1.5 s 段的收益保持（C 新修好 7 / 新弄坏 1；B 是 9 / 0）⇒ **形状确实由机制决定**；
2. 但 **C 相对起点只动了 1 个字符**（123 → 122），配对检验也判为无证据（offset_long −5.02 ms，p=0.19 / 0.51）⇒ 它相对 A 的优势主要是**躲开了 A 自身的退步**（A 退到 129），不是把长音修好了；
3. 短条目代价要分成两部分：再平衡本身（C 的 offset_short +0.92 ms、终态全量 MAE +2.51 ms）与 B 的复制稀释（最短段弄坏 4 vs 2）⇒ **我无法把代价单独归给某一方**，这句话如实写在这里。

### 收口
- 按 C 预注册 §3 的判定（Δ ≤ −10 ms 且两检验 p<0.05）⇒ **H-C 不成立**；
- 今晚两次独立干预（B 改采样、C 改目标权重）**形状两次命中、幅度都没超出起点**⇒ 暴露/倾斜的机制描述是对的，但在现有解码头下用它做再平衡，收益低于短条目代价；
- 唯一未试的方向是**结构改动**（时间戳头输出「起始 + 时长」，或给结束点单独的解码/回归分支），需独立立项。
