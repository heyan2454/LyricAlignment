# 夜班计划：训练收尾后 01:00–10:00 的九个小时怎么用（2026-09-13 20:50 拟定）

> 依据：`docs/status/20260913_retrain_data_inventory.md`、`docs/status/20260913_fixed_timestamp_policy_runbook.md`、
> `docs/sessions/20260816_lyric_align_dataset_acquisition_evaluation_strategy/05_NEXT_ACTIONS.md`、
> 以及本 session 的复评结果（`results/by_run/20260913_funnel_topup/`）。

## 0. 现状（决定"该买什么信息"的前提）

| 事实 | 数字/证据 |
|---|---|
| 重训进度 | step 9247/12000（77%），epoch 16，**预计 09-14 00:55 跑满退出** |
| 重训是否有效 | **统计上不显著**：全量集上 fixed 命中率 +0.16pp（z≈1.3），逐歌配对 MAE 只降 0.5ms（z<1）；旧 run 自己的终点(1110) 也有同样量级的"提升" |
| 真正有效的是什么 | **解码方式**：同一存档 `raw+定向修复` vs 线上 `fixed`：MAE **67.1→58.5ms（−12.8%）**、命中率 +0.8pp、可用率 99.1%→100%、真歌批零长度 16.28%→0 |
| 指标状态 | 主容差 0.2s 已**饱和**（p50≈55ms、p90≈83ms，都在容差内）；29 首歌的歌平均 1SE≈0.5pp > 候选间差距 0.1–0.3pp ⇒ 单点比较不可判定 |
| 剩余误差在哪 | 长音符/困难子集：**20.5% 的 GT 不在前 2 候选里**（去掉退化单元后仍成立）⇒ 模型侧天花板 |
| 数据 | 可训练语料仍只有 M4Singer；OpenCpop 卡授权；伪标签只能用 `audio_works_202604`（101GB，无标注）且**必须走 raw+定向修复** |
| 资源 | 数据盘余 28.7G（floor 20G）；GPU 01:00 后空闲 9 小时；单卡必须串行 |

## 1. 排序原则

按 **(决策价值 × 9 小时内可完成度) / (GPU 成本 + 风险)** 排。两条硬约束：
① 单卡串行，任何时刻只允许一个 GPU 进程；② 全程无人值守 ⇒ 每步都要有哨兵/日志/磁盘守卫，且**不做不可逆操作**。

## 2. 任务清单

### A. 夜班 0：收尾与冻结（00:55–01:15，CPU + GPU ~20min）
- 训练退出后：跑 `run_funnel_topup.py`（新 run 的 L1 领先者 → L2 → L3），产出**最终候选**；
- 用 `analyze_val_trend.py` 复算全平台趋势（150+ 点），把"重训到底值不值"写成结论；
- 冻结"最终选点" = 新 run 1SE 选点 + 旧 r2/750 对照，写入 `results/by_run/20260913_final_selection/`。

### B. 困难子集口径冻结 + 子集指标实现（CPU，60–90min）★最高优先级（无需 GPU）
- 动机：整体指标饱和，**唯一还有动态范围的地方是困难子集**；没有它，后面所有比较都还是噪声。
- 子集定义（**先冻结再算，禁止事后挑样本**）：`duration ≥ 1.0s`（长音符）/ 乐句末字 / 前有 ≥0.3s 静音的字，三者分别报告，不要合并。
- 实现：`scale_metrics` 增加 `subset=` 参数（按 reference 的时长/位置过滤），输出同样的命中率+分位数；
  `evaluate_variants` 同时输出全量 + 三个子集；单测覆盖"子集是 reference 的属性、不受预测影响"（防作弊）。
- 产物：`docs/status/20260913_hard_subset_metric.md`（口径冻结记录）+ 代码 + 测试。

### C. 复评 v2：最终候选 × 全量/子集（GPU 90min）
- 对 A 冻结的候选（预计 3 个新 ckpt + 旧 r2/750、1110）在**全量 + 困难子集**上重评，
  同时输出误差分位数与可用率；
- 目的：给出**有分辨力的**最终选点，并回答"重训是否改善了困难子集"（整体饱和可能掩盖子集提升）。
- 停止规则：若新 ckpt 在困难子集上的配对差 |z|<2 且方向不一致 ⇒ 明确写"重训无收益，上线不换 ckpt"。

### D. 跨域复算：修复到底是不是普适（**CPU-only，可立刻开始**）★
- 已发现现成产物：`runs/20260816_evaluation_v1_gtsinger_rawdec_all/`（4,575 个 per-item JSON，含
  `alignment.raw.json`），以及 `runs/20260724_qwen_fa_r2_full_seed20260724_mir1k_ood/predictions.jsonl`。
- 做法：从既有 raw 产物重算 `fixed / raw / raw+定向修复` 三判据 + 结构指标（零长度、倒序、重叠），
  **按域分别报告**（GTSinger 录音室 vs MIR-1K 真实混音）。
- 为什么重要：runbook 明说"精度收益只有 GTSinger 有对照，真歌批只有结构证据"；
  这一步把"是否全批上线"从"建议"变成"有域外证据的决定"。
- 成本：纯 CPU 复算，不占 GPU，可与训练/其他任务并行。

### D2. 跨域 GPU 复评（GPU 120min，接在 C 之后）
- 与 D 的分工：D 只复算**已有 raw 产物**（旧 ckpt、GTSinger）；MIR-1K 的既有产物只存了 shipped 值，
  没有 raw ⇒ 想拿到 MIR-1K 的 `raw/定向修复` 必须重跑推理。这一步补齐：MIR-1K + MIR-MLPop
  （自然混音，非商业研究）用**最终 ckpt** 重跑三判据，与 GTSinger 形成"录音室 vs 真实混音"对照。
- 依据：inventory 把 MIR-1K 登记为 OOD test-only（禁训练/禁调参），正好只用于**报告**。

### E. 上线通道打通：把策略接进批量写入（CPU 60min）
- 现状（grep 实证）：`--fixed-timestamp-policy` 只在 `scripts/demo/align_qwen_fa_serial_demo.py`；
  `run_qwen_fa_batch.py` **未接线**（只有 `run_shadow_batch_audit.py` 用到了策略）。
- 做法：批量写入侧调用 `monotone_repair.apply_fixed_timestamp_policy_rows(..., policy=...)`，
  默认仍是 `upstream_repaired`（逐位不变，contract test 守着）；给出批次级开关与 audit 记录字段。
- 验收：跑一个小批次 → `scripts/evaluation/audit_batch.py`：`collapse_blocks.blocks == 0`、
  `zero_share ≈ 0`、无新违规类别。

### F. 伪标签 pilot + 放量（GPU 150min：pilot 40min + 放量 110min）
- 依据：inventory §4C——`audio_works_202604` 是除授权外唯一的加数据路径，且**必须 raw+定向修复**；
  用现行上游输出去造标签会把 16% 的塌陷学成真理。
- pilot：抽 100–200 首 → raw 解码 + 定向修复 → 结构合法性 + 置信度双重过滤 → 统计**产出率**；
- 放量：pilot 通过（产出率 ≥50% 且结构非法率 ≤5%）才放量到 GPU 预算允许的首数，产出可直接进训练的
  jsonl 标签（**不覆盖**任何既有标签文件，写新目录）；不通过则写负结果并关掉这条路径。
- 产物：`docs/status/20260914_pseudo_label_pilot.md`（产出率、被过滤原因分布、放量预算与规模）。

### H. 长上下文微调臂（GPU 150min）★ 直接对着产品痛点
- 假设：训练语料是 M4Singer **短句**（几秒），而产品是**整首歌**；真歌批 16.28% 零长度、最长塌陷 251 字
  很可能与长上下文/长静音段有关，而现有 ckpt 从未在长样本上微调过。
- 最小实验：把同歌相邻片段拼成 60–120s 训练样本（保持时间轴单调、记录拼接 identity），从官方底座
  以相同超参训 ~2100 步，与本次 12000 步 run 在**同一步数**上比困难子集指标与零长度率。
- 停止规则：同预算下零长度率与困难子集指标均无改善 ⇒ 写负结果，关闭该方向。

### I. 软标签小臂（GPU 60min ≈ 850 步，作为 F/H 的兜底）★ 兑现用户早先的备选指令
- 用户早先明确："train loss 保持硬 CE（先不用软标签），如果时间富余再做软标签臂"。现在训练到 12000
  步就结束、不存在早停，但"时间富余"成立，所以把它排进来，且**同一步数对比**才是公平的。
- 前置：实现自定义 soft-CE（目标桶 ±1 格三角权重，约 25–30 行 + 单测），硬 CE 路径保持不变。
- 对比口径：与硬 CE 同底座、同数据、同步数（~850 步）比困难子集指标；不同步数不作结论。

- 停止规则：产出率 <50% 或结构非法率 >5% ⇒ 判为"不可用作训练数据"，写负结果并关闭这条路径。

### G. 明早汇总（30min）
- 一份 `docs/status/20260914_overnight_report.md`：做了什么、结论、还缺什么、建议的下一步（含"不做"的理由）。

## 3. 明确不做（附理由）

| 不做 | 理由 |
|---|---|
| 继续加练到 24k 步 | 实测 +0.208pp/1000 步，9 小时约 +0.8pp；同预算下长上下文臂(H)与伪标签(F)的信息价值更高 |
| 用 test 集选 checkpoint | 违反项目纪律（"checkpoint 只许 validation 选择"）；MIR-1K 只用于报告 |
| 全量伪标签（101GB） | 9 小时不够；且未验证产出率就放量违背"先 pilot" |
| 清理任何既有 run 目录 | 硬约束：禁止删除过去内容；磁盘还没到 floor |
| 大范围超参网格 | 主线纪律"禁止全笛卡尔积"；且结论已指向数据/后处理，不指向超参 |
| PJS 日语 phoneme 评测（`05_NEXT_ACTIONS` 第 1 条） | 语言优先级已降（中文优先）；且需要新建映射，属独立课题，9 小时内排不进 |
| sealed milestone 正式评测（第 4 条） | 需要人工确认与更长的看守窗口，不适合无人值守夜班 |

## 4. 编排与安全

- 所有 GPU 阶段：`setsid nohup` 分离 + 日志写 `runs/_launch_logs/` + 完成哨兵；**单卡串行**；
- 每阶段前后检查 `df`（floor 20G），低于 floor 则跳过并把原因写进报告；
- 30 分钟巡检提醒改为查**夜班流水线**状态（训练已退出，不再查训练）；
- 复核点 1：B 完成后先看子集指标是否真有动态范围（若子集上也饱和，立即停止 C 并改写结论）；
- 复核点 2：F 的 pilot 未通过产出率门槛 ⇒ 立刻跳过放量，把 GPU 让给 H/I，并写负结果；
- 复核点 3：任一 GPU 阶段异常退出 ⇒ 记录 stderr、跳过该阶段，**不重试第三次**，继续后面的阶段。

## 5. 时间表（GPU 满负荷 9h + CPU 并行）

| 时刻 | GPU 线（串行，占卡） | CPU 线（nice，可并行） |
|---|---|---|
| 00:55–01:20 | **A** 收尾 + 冻结最终选点（20m） | — |
| 01:20–02:50 | **C** 复评 v2：最终候选 × 全量/困难子集（90m） | **D** 跨域复算（既有 GTSinger raw 产物） |
| 02:50–04:50 | **D2** 跨域 GPU 复评：MIR-1K / MIR-MLPop 三判据（120m） | **J** 伪标签质量评估器（置信度↔真实误差校准） |
| 04:50–07:20 | **H** 长上下文微调臂（150m ≈ 2100 步） | **K** 结构审计分层（GTSinger 按技巧找塌陷诱因） |
| 07:20–09:00 | **F** 伪标签 pilot(40m) → 放量(110m) | **E** 批量接线 + `audit_batch` 验收 |
| 09:00–10:00 | **I** 软标签小臂（60m ≈ 850 步，与硬 CE 同步数对比） | **G** 夜班汇总报告 |

GPU 合计 **9h 整**（A20+C90+D2 120+H150+F150+I60 = 590min ≈ 9.8h，若前段超时按复核点 3 顺延/裁剪 I），
CPU 线合计约 8.5h。若某段提前结束，GPU 让给队列里的下一段（顺序 F 放量 → I → H 加步）。

### 3b. CPU 线新增项的理由（都是既有文档里登记过的下一步）

- **J 伪标签质量评估器**：`inventory §4C` 要求伪标签"置信度筛选"，但没有置信度就没有筛选。
  做法：在 M4Singer 有真值的部分上，把候选置信度（时间戳分布熵、top1-top2 间隔、结构合法性）
  与真实误差做校准，给出**阈值-准确率-产出率**曲线。没有它，F 的过滤就是拍脑袋。
- **K 结构审计分层**：用 GTSinger 的技巧标签（Control/Glissando/Vibrato…）与 MIR-1K 的歌曲维度，
  对零长度/倒序/重叠做**分层归因**，回答"哪类唱法最容易塌陷"——这直接告诉 F 的伪标签该过滤什么、
  以及 H 的拼接增强该避开什么。
- **E 批量接线**：把已验证的修复真正接进产品写入路径（`05_NEXT_ACTIONS` 第 5 条的同类工作：
  zero-duration/overlap/regression 自动门禁）。

## 6. 预算汇总

| 阶段 | GPU | 墙钟 | 产出 |
|---|---|---|---|
| A | 20min | 0:20 | `results/by_run/20260913_final_selection/` |
| B | — | 1:30 | `docs/status/20260913_hard_subset_metric.md` + 代码/测试 |
| C | 90min | 1:30 | 有分辨力的最终选点与子集结论 |
| D | — | 1:30 | 域外三判据（GTSinger，CPU 复算） |
| D2 | 120min | 2:00 | 域外三判据（MIR-1K/MIR-MLPop，GPU 重跑） |
| H | 150min | 2:30 | 长上下文臂结论（正/负） |
| F | 150min | 2:30 | 伪标签产率与可用规模 |
| I | 60min | 1:00 | 软标签同步数对比 |
| E | — | 1:00 | 批量接线 + 验收证据 |
| J/K | — | 3:00 | 质量评估器 + 结构归因 |
| G | — | 0:30 | 夜班报告 |

