# 明早索引（09:17 生成，10:00 前读这份就够）

> 所有数字都由脚本从 `results/by_run/**` 的 JSON 生成；不要从任何 .md 里手抄数字回文档。

| 顺序 | 文件 | 是什么 |
|---|---|---|
| 1 | `docs/status/20260914_plain_summary.md` | 先看这份：大白话，无术语，数字由脚本从 JSON 生成 |
| 2 | `docs/status/20260914_night_report.md` | 技术总报告（20+ 节，每节指向自己的数据源；缺数据显示「未跑」） |
| 3 | `docs/status/20260914_concat_arm_prereg.md` | 预注册主线：§3g-2 预测带、§3i 功效上限、§3j 数据路径关闭、§3l 2×2 决策表、§3n B 的覆盖混杂、§3p 实际填格 |
| 4 | `docs/status/20260914_c_arm_prereg.md` | C 臂（字符级 loss 加权）预注册 + §5 可 falsify 的形状预测 |
| 5 | `docs/status/20260914_robustness_B_vs_A.md` | B vs A 三关稳健性（中位/截尾 + McNemar + ×4 校正） |
| 6 | `docs/status/20260914_robustness_A_vs_start.md` | 对照臂漂移的同一套检查（结论：只是迹象） |
| 7 | `docs/status/20260914_robustness_750_vs_12000.md` | 整条训练史的同款检查（短音改善=确证；长音改善=无证据） |
| 8 | `docs/status/20260914_position_effect.md` | 末字位置效应：被时长混杂解释，不做尾部延长改动 |
| 9 | `docs/status/20260914_gating_calibration.md` | 按预测时长分档的门控阈值：三个预算一致更差 ⇒ 关闭 |
| 10 | `results/by_run/20260914_ab_decision/REPORT.md` | 决策表程序输出的所在格与下一步 |
| 11 | `results/by_run/20260914_long_context_view/` | 五判据长流视图全部存档（含 A/B/拼接/基线） |
| 12 | `results/by_run/20260914_arms_by_bucket/metrics.json` | 时长分段的逐字符计数表（形状与量级） |
| 13 | `results/by_run/20260914_exposure_fit/validation_uniform_f8.json` | 暴露—误差拟合（采用口径：留出曲线 + 臂真实 factor=8） |
| 14 | `results/by_run/20260914_real_song_decoder/metrics.json` | 真歌端到端 official vs DP（压力条件；含两轴位移） |
| 15 | `results/by_run/20260914_review_gating/metrics.json` | 置信度门控交付物的实测数字 |

## 现在正在跑什么

- C 臂（字符级 loss 加权，slot=offset，600 步）在 GPU 上，约 09:44 结束；
- 它的 CPU 评测链（逐字符 dump → 机制画像 → 两组三关稳健性 → 同步数屏幕）已挂哨兵，会自动跑；
- 之后需要手动做的只有一件：C 的长流视图评测（约 6 分钟 GPU，必须等 C 训练退出后跑，保持单卡串行）。

## 今晚的三条硬结论（细节见上述文件）

1. **免费可拿**：DP 解码（8 个独立存档上逐个现算：+0.90 ~ +1.17 pp）与置信度门控（复核 10% 消除 ~55% 缺陷）；但两者必须一起上，因为 DP 会把结构探针全部变绿。
2. **机制**：误差集中在长字符结束点；典型预测并不收缩，收缩是失败子集的条件性偏差；不确定性随时长急剧集中（最低置信四分位占比 14%→80%）。
3. **B 臂（条目复制）净收益为零**：时长分段形状完全命中预测（≥1.5s 新修好 9、最短段新弄坏 4），但总分 123 = 起点 123，而对照 A 退到 129 ⇒ 条目复制不是可用的工程手段，C 是无混杂的下一步判定。

## 结论强度分级（即使 C 臂还没跑完，这份报告也能读）

**A. 已确证（有通过校正的检验或跨存档一致）**
1. 从官方底座重训在**短字符结束点**上确证改善（配对 −2.86 ms，McNemar 39/17，×4 校正后 p=0.018）。
2. DP 解码的增益跨 **8 个独立存档**一致，区间 +0.90 ~ +1.17 pp（由脚本逐个现算），可用率恒 1.0000。
3. 置信度门控跨歌泛化：按歌留一，复核 10% 消除约 55% 缺陷（工具已交付）。
4. 误差集中在长字符、且随时长单调上升（留出口径 1.1% → 10.2%）。
5. 长字符失败子集被压短且偏早（长度比 0.538、中点 −490 ms），而**全体统计看起来正常**。

**B. 已否证（关掉的下一步，别再走）**
- 同 logits 内重排、约束解码修精度、跨窗共识、事后时长校准、按预测时长分档的门控阈值、
  末字尾部延长、加数据补长音暴露、把短句拼成长流训练（中性）。

**C. 只是迹象 / 边界结论（措辞必须带限定）**
- B 臂在长字符结束点上 Δ −20.7 ms（未校正 p=0.006/0.021；×4 校正后二项 p=0.085）
  ⇒ 按预注册单一主判据可读作"支持暴露假设"，按保守口径只能读作"迹象"；
  两种口径的动作相同（都指向 C），但**表述不能合并**。
- A 臂在长音上的退化（+9.55 ms，中位与截尾均值 0）⇒ 迹象，未过校正。

**D. C 臂已判定（10:10，见 `20260914_c_verdict.md`）**
- **C 相对起点没买到长音改善**：offset_long Δ = −5.02 ms（均值 p=0.19、二项 p=0.51 ⇒ 无证据）；
  相对 A 的 −14.57 ms 全部来自 **A 自身在长音上退步**，不是 C 变好。
- 代价是真的且比 B 更大：短条目屏幕 −0.29 pp（0/6 点更好）、终态 MAE +2.51 ms（B 为 +1.29 ms）。
- **关键点**：C 不复制条目 ⇒ 没有 B 那个可甩锅的覆盖副作用 ⇒ 这次不能再把"没收益"归因于工具。
- ⇒ **在这个架构上，改采样或改目标权重都换不来长音精度、却要付短条目代价 ⇒ 数据/目标层面的
  再平衡已走到边界**；下一步只剩结构改动（时间戳头改"起始+时长"参数化，或给结束点单独的解码分支），
  需独立立项。
## 存储核查（09:42 实测，回应"存储预算不多、严禁爆服务器"）
| 项目 | 实测 | 判定 |
|---|---|---|
| `.git` 总体积 | 157 MB | 主要是历史里的旧大文件（项目纪律禁止清理既有内容，故不重写历史） |
| git 中 `results/` | **11.8 MB / 216 个文件** | 健康；今晚新增的判决/稳健性/计数文件都是 KB 级 |
| 工作树里三个 23 MB 长流视图 JSON | **未被 git 跟踪**（被 ignore 规则挡住） | 只占磁盘 70 MB，不进仓库；tracked=否 已逐个确认 |
| 逐字符 dump（12–17 MB/个） | 由 `.gitignore` 排除，仅指标 JSON 入库 | 符合"大型 per-item 数据外置"约定 |
| 磁盘可用 | 22 GB（训练器 floor 22G 已到 ⇒ 自动转仅权重存档，实测两臂 + C 合计新增约 2.6 GB） | 安全 |

复核命令（一条）：
`du -sh .git results && git ls-tree -r -l HEAD -- results | awk '{s+=$4} END {printf "%.1f MB (%d files)\n", s/1024/1024, NR}'`
以及 `git ls-files --error-unmatch <路径>` 判断某大文件是否真进了仓库（不要只看 `du`）。

处置决定：那 70 MB 未跟踪文件**保留不删**（体积可忽略，且它们是若干结论的原始载体）；
如果之后磁盘真的吃紧，优先删这类"有 .summary.json 孪生文件"的大对象，而不是任何指标 JSON。

## 待评审的既有代码问题（17:15 用 AST 扫描发现，我未修改）
- `src/lyricalign/datasets/m4singer.py:251`：`return` 之后还有一段分组逻辑（永不执行）。
  可能是真 bug，也可能被有意停用；按"不改写既有内容"的纪律**留给评审决定**。
- `scripts/research_v7/collect_evidence_bundle.py:136`：`return` 之后是遗留注释块与陈旧的
  "28 passed" 字样，属卫生问题。
- 我自己今晚引入的同类问题（DP 身份扩展写成死代码）已修复 + 3 项契约测试（提交 `59418c9`）。
