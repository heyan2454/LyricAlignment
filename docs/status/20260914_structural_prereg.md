# 结构改动立项与预检（2026-09-14 15:54，用户授权自主决定变体）

## 0. 背景：为什么"起始 + 时长"这个变体被预检挡住
零 GPU 的误差分解（`results/by_run/20260914_mech_validation_uniform/per_character.jsonl`，
只含验证集 6,415 个字符）：

| 时长桶 | n | \|起始点误差\| | \|结束点误差\| | \|隐含时长误差\| | corr(结束,起始) | 扣掉起始漂移后的结束点残差 |
|---|---|---|---|---|---|---|
| 0–0.5s | 4,260 | 25 ms | 25 ms | 30 ms | +0.10 | 30 ms（1.20×） |
| 0.5–1s | 1,215 | 25 ms | 25 ms | 40 ms | −0.01 | 40 ms（1.60×） |
| 1–2s | 813 | 25 ms | 35 ms | 40 ms | +0.13 | 40 ms（1.14×） |
| **≥2s** | 127 | 25 ms | 35 ms | **60 ms** | +0.01 | **60 ms（1.71×）** |

⇒ 结论 1：结束点误差**与起始点误差无关**（相关 ≈0，扣除后残差不减反增）
⇒ "把结束点锚到起始点上"不能解释当前误差来源。
⇒ 结论 2：模型**自己推算出的时长比它直接预测的结束点更差**（≥2s：60 vs 35 ms）。
把第二个槽位改成预测时长，等于把负载压到一个目前估得更差的量上。
⇒ 因此"起始 + 时长"参数化**没有预检支持**（但仍值得花一次臂去验，见 §3 的判据）。

## 1. 三条候选与它们各自需要回答的问题
| 变体 | 机制假设 | 先决问题（预检） | 实现代价 |
|---|---|---|---|
| A. 起始 + 时长参数化 | 让第二个头预测时长而非绝对 bin，可能更好学/更可校准 | 时长信息是否在表示里、只是被读错？ | 小（只改标签与解码，无输入变更） |
| B. 两段式条件预测 | 先定起始点，再以其为条件预测结束点 | 需要把起始 bin 作为**输入**喂进去 ⇒ 要扩输入词表/embedding，**不是中等代价，是真正的架构手术** | 大 |
| C. 结束点专门的解码分支 | 结束点用独立的转移/约束（如相邻共享边界的联合约束） | DP 已覆盖大部分（吸附 ≡ DP，已否证） | 小但先验不足 |

**本轮不再凭感觉选**：先做一个读法/表示的判别实验（§2），它直接决定 A 还是 B。

## 2. 判别实验（读法 vs 表示，零训练）
问题：长音的**真实时长**能否从模型**现有的输出分布**里被较好地恢复？
- 取验证集含长字符的 808 条条目，导出每个字符**结束点槽位的完整 top-k 分布**（不只 argmax）；
- 在其上拟合若干**只用分布、不用真值**的时长估计器：argmax bin、分布均值、分位数、
  top-k 支持集内的最大/最小 bin、以及一个线性探针（ridge，仅用 top-k 的位置与概率）；
- 判据（预先写死，用留出歌交叉验证）：
  - 若**某个读法把 ≥2s 的时长误差从 60 ms 显著降到 ≤35 ms**（与现有结束点误差同量级）
    ⇒ 信息在输出里、只是被 argmax 读错 ⇒ **属于解码/校准问题**：先做便宜的读法修复，
    并把 A 降级为次要候选；
  - 若**最好的读法也降不下来** ⇒ 当前表示对长音时长信息不足 ⇒ 只有 B（架构改动）值得一试，
    A 与一切再平衡都属于错的方向；
  - 介于两者之间 ⇒ A（时长参数化）作为一次 6000 步的对照臂值得跑，与 `uniform-6000` **同步数**比较。

## 3. 若进入训练臂（A）：设计与停止规则（预先写死）
- 起点：与均匀臂**同一个 r1 checkpoint**、同样的批大小/LR/cycle，`max_steps = 6000`，
  比较对象 = `uniform-6000` 存档（同步数等算力）；
- 唯一变量 = 第二个槽位的语义（绝对结束 bin → 时长 bin），解码端相应还原；
- **主指标**：验证集 ≥1s 长字符**结束点**超 0.2s 率的逐字符配对差（§3i 功效：n≈950、SE≈5.3 ms，
  可检 ±10.5 ms）；三关判定同 §3g-4（Δ ≤ −10 ms 且两检验过 ×K 校正）；
- **守护指标**：短字符（0–0.5s）超差率恶化不超过 +0.3 pp；
- **停止规则**：6000 步若主指标未过三关 ⇒ 结构参数化路线关闭，不再加步数、不再调权重；
  若中途 L1 崩塌（相对对照 −1 pp 以上）⇒ 提前停止并写负结果；
- 预算：≤ 7.5 小时 GPU（6000 步 + 漏斗评估），磁盘门禁 ≥15 G，单卡串行，不删既有产物。

## 4. 现在不做的事
- 不再加第四个"再平衡"臂（B/C 两条已收口，§3l/§C 判定线明确）；
- 不碰测试条目（选点防火墙已在工具里强制）；
- 不把 DP 或门控直接写进产品代码路径 —— 它们需要一次评审级决定（是否纳入 `model_identity()` 校验链）。


### 4b. 新增关闭项：**选择性**时长校准也不行（18:06 实测，脚本 `selective_duration_recal_test.py`）
既然全局单调校准会伤到多数（原始隐含时长**中位误差 = 0 ms**、秩相关 0.92），
剩下的希望是"失败集中在模型自己不确定/候选偏长的字上"。只对这些字做校准（只用模型输出，不用真值）：

| 触及比例 | ≥2s 原始超差率 | 选择性校准后 | Δ |
|---|---|---|---|
| 5.8%（最不确定 10% 且候选≥0.8s） | 6.06% | **9.09%** | **+3.03 pp（更差）** |
| 10.7%（最不确定 20%） | 6.06% | 9.09% | +3.03 pp（更差） |

⇒ **事后时长校准的两条路（全局 / 选择性）都关闭**。
确切原因（比原来的"无收益"说法更准）：**失败是个体性的，不是单调尺度偏差** ——
多数字符的时长本来就预测对（误差中位 0），任何只用分布的单调映射要么碰不到失败点，
要么把已经正确的多数一起挪坏；而高熵并不等于"时长偏短"。
⇒ 这条与 §2 的读法判别合起来说明：**要修长音，必须改变模型学到时长的方式**（参数化/结构），
   不能靠事后处理 —— 也正是本臂存在的理由。

## 5. 时长参数化臂的判决规则（16:46 登记，早于它跑出的任何数字）
配置 `configs/training/qwen_fa_lora_duration_target_20260914.yaml`：从 `uniform-12000` 热启动、
**6000 步、完整 LR（projector 2e-5 / lora 5e-5）、两个 cycle**、保存每 500 步；
`timestamp_target=duration` ⇒ 训练器**自动禁用**漏斗与终态验证并写 `validation_status.json`
（因为那两个指标会把时长槽位当绝对位置读；CPU smoke 已验证这一行为）。

**主指标（唯一）**：验证集 ≥1s 长字符**结束点**超 0.2s 率的逐字符配对差，
比较对象 = 它自己的起点 `uniform-12000`（同一批 808 条留出条目、同一 `--sample-seed`）：
- 判定成立：Δ ≤ −10 ms 且 p_t×K < 0.05 且 McNemar×K < 0.05（三关，同 §3g-4）；
- |z| < 2 ⇒ 写负结果，**结构参数化家族一并收口**（不再加步数、不再调 LR 重跑）；
- 变差（Δ ≥ +10 ms 且过三关）⇒ 参数化本身有害，立即关闭并保留读数。

**已知混杂（如实登记，不许事后改口）**：
1. 没有"同样 6000 步、绝对目标"的对照臂（GPU 预算只够一条臂）。因此若读数变好，
   其中一部分可能来自"多训了 6000 步"而非参数化。缓解证据：A 臂在 600 步时长音配对为
   +0.53 ms（无变化）、B/C 的总数都停在起点附近 ⇒ 多训同目标带来的漂移很小，
   但**这不是证明**；若本臂读数显著变好，下一步必须补一条 6000 步绝对目标对照再谈配方。
2. 解码端只有 `raw`（argmax，end = start + duration）在 duration 模式下有意义；
   `fixed`/`dp` 会被显式标为 `not_supported_in_duration_mode`，不允许拿它们的数字做任何比较。

**磁盘与算力**：6000 步 ≈ 7.5–8.5 小时；save_steps=500 ⇒ 12 个存档；
到 disk_floor 后训练器自动转仅权重存档；不删任何既有产物；单卡串行。

## 6. 中途健康探针（17:41 登记）——只用于资源判断，绝不用于选点
早期探针（`step-000500`，150 条 CPU 导出，与起点 dump 同 seed 同顺序 ⇒ 可逐字符配对）：

| 项目 | 读数 |
|---|---|
| 起始点误差 | 中位 30 ms（仍正常），**p90 1395 ms（部分条目的起始点被冲坏）** |
| 结束点误差（按 duration 还原后） | 中位 1120 ms（起点 35 ms） |
| 短字符（<0.4s）诊断 | 真结束点中位 2225 ms，误差中位 1120 ms ⇒ 误差与**绝对位置同量级** |

⇒ **不是我的还原公式错**（若公式错，短字符误差应≈2×位置而符号混乱；这里量级与"仍输出绝对 bin"一致）：
   step 500 时第二槽位仍在绝对位置附近，属热启动惯性，500 步不足以完成语义切换。
⇒ 但这意味着 §5 的判决有一个新前提：**必须确认到 6000 步时臂已经收敛**，
   否则"结构参数化无效"这个结论其实是在说"6000 步不够换语义"。
   处理办法：登记 1000 / 2000 / 3000 / 4500 步的趋势探针（CPU、nice、60 条），
   只记录两条数：**起始点误差中位** 与 **结束点误差中位**。
   - 起始点误差是 duration 参数化下结束点误差的**硬下界**（end = start + dur）：
     若起始点不回落到 ~30 ms 量级，那么即便时长完美，本臂也注定失败 ⇒ 这是最有价值的一条趋势数；
   - 探针**不参与任何选点**（选点规则仍是固定 6000 步），只用于判断"要不要让它跑完"。

### 6b. 探针链自身的一个静默故障（17:41，值得记下来）
第一版趋势 watcher 用 `printf step-%07d` 生成存档路径，而实际存档是 6 位（`step-000500`）。
⇒ watcher 会**永远等不到目录、不报错、最后静默跳过全部探针**，明早看起来就像"我没做健康检查"。
另外我第一次"杀掉旧 watcher"只杀掉了启动器，setsid 脱离的子进程仍在跑 ⇒ 短暂出现**两个 controller
并发等同一个输出根**（违反"禁止两个 controller"）。已按精确 PID 停止，现存唯一 watcher。

规则化：**启动任何等待型脚本之前，先 `ls` 一次它要等的真实路径**，把格式抄进脚本而不是凭记忆写；
停止后台任务时按 `ps -eo pid,ppid,args` 找全部相关 PID，不能只杀看到的第一个。

### 6c. 修掉 duration 模式下的**标签口径错位**（17:46）
`measure_predicted_boundary_acoustics.py` 里的 `label_bins` 取自标签文件的**绝对结束 bin**，
而 duration 模型第二槽位输出**时长** ⇒ `label_rank / top5_mass / mass_in_tol` 全部按错 bin 计算
（argmax 误差不受影响，所以我第一次配对分析没被发现）。
修复：duration 模式下复用训练端同一个 `duration_target_class_ids` 换算标签（避免两处实现漂移）。
验证方式（这次修好的过程本身也是一条证据）：同一个 step-500 存档，
用错口径时"真值排第 1"占 **18%**，用正确时长口径只有 **5%**
⇒ **模型的第二槽位分布仍压在绝对位置上**，与 §6 的过渡期诊断一致；
（3 条 vs 808 条的样本差已注明，这条比较只用于定性说明口径方向，不作为效果数字。）

过程记录：我第一次把定义 `timestamp_target` 的插入点用错了锚点（那是另一个脚本的写法），
结果替换没落地 ⇒ **绝对模式回归跑立刻以 NameError 抓住**。
再次说明：**每次改完都要跑一次"确认没破坏既有路径"的最小回归**，退出码不等于语义正确，
但没有回归连这种低级错误都会溜过去。

### 6d. 趋势判据落地为工具（17:48）
`scripts/evaluation/summarize_duration_trend.py` → `docs/status/20260914_duration_trend.md`（机器生成，探针一到就更新）。
它额外算一个**判别数**：`spearman_duration` = (第二槽位−第一槽位) 与真时长的秩相关。
- 若它随步数升到接近 1 且结束点误差下降 ⇒ 语义切换完成 ⇒ 判决可信；
- 若到 3000–4500 步仍接近 0 ⇒ **本轮回答的不是"参数化有没有用"，而是"6000 步够不够换语义"**，
  报告必须这样写，不能写成"时长参数化无效"。

第一个点（step 500，1,248 字符）：起始中位 30 ms / p90 1190 ms、结束中位 1165 ms、超差率 81.7%、
估时长中位 1.12 s（真 0.40 s）、**spearman_duration = −0.03**。
⇒ step 500 时第二槽位**不含时长信息**，与 §6/§6c 的过渡期诊断一致。

### 6e. 两个分支的处理规则（17:52 登记，早于任何趋势读数）
趋势判据引入后，判决可能出现三种情形，各自的处理**现在就写死**：

1. **`spearman_duration` 随步数上升（>0.5）且结束点误差接近起点水平** ⇒ 语义切换基本完成，
   §5 的配对判决可用；结论按"参数化在本数据上有效/无效"表述。
2. **到 step 4500 `spearman_duration` 仍接近 0（判据：|ρ| < 0.2 且结束点中位误差 > 200 ms）**
   ⇒ 这一轮**没有回答**"时长参数化有没有用"，它回答的是"从绝对语义热启动、6000 步不够换语义"。
   报告必须按这句话写；**不得**写成"结构参数化无效/被否证"。
   处理：如实登记为 ambiguous-null，并明确"若要判定参数化，需要另一条臂（更长步数或
   第二槽位随机初始化）"——**是否投这条臂交给用户决定，不自作主张延长**（预算与优先级由人定）。
3. **起始点误差没有回到 ~30 ms 量级（判据：中位 > 60 ms 或 p90 > 300 ms）**
   ⇒ 该臂**无效而非不利**：在 duration 参数化下 end = start + duration，起始点误差是结束点误差的
   硬下界，起始点坏掉意味着即便时长完美也不可能赢。
   处理：报告为"本臂不可用于判参数化"，并把成因（完整 LR 热启动冲坏了起始点头）写清楚；
   这属于实验设计缺陷，要算在我头上，不能算在假设头上。

另外预先声明：**不因为中途读数不利而提前停止训练**（只有 §3 的崩塌保护才停）；
也**不因为中途读数有利而改判决线**。趋势点只用于：识别情形 2/3、以及让最终表述诚实。

### 6f. 判决链口径审计（17:54）
逐个检查今晚判决链调用的工具是否认识 duration 语义：
- `measure_predicted_boundary_acoustics.py`：**认识**（还原 end = start + duration、并换算标签，见 §6c），
  且链上调用它时传了臂自己的 `--config`；
- `paired_robustness_check.py` / `arms_by_bucket_counts.py` / `ab_decision_table.py`：
  只消费 dump 里**已换算**的 `abs_err_argmax / signed_err / duration` 字段 ⇒ **与参数化无关**。

⇒ 结论：**语义换算只发生在一处（导出工具）**，这是有意的设计；
   因此不会出现"上游修了、下游还在按绝对位置数数"的第二处错位。
   反过来也说明：如果哪天忘了传臂自己的 `--config`，整条链会**静默按绝对语义算**——
   所以判决链里的 `--config "$CFG"` 是单点关键，改动这条链时必须复查它。

## 7. 若本轮结论落在 §6e 情形 2（步数不够），下一轮的**最小可信设计**（先写死不实施）
当前缺失的对照只有一个：**同样热启动、同样 6000 步、但保持绝对语义**的臂。
没有它，任何"变好/变差"都混着"多训了 6000 步"这一项。

最小可信设计（若用户批准再跑，预算 ≈ 7.5 小时 GPU）：
- 两臂同起点（`uniform-12000`）、同步数（6000）、同 LR/cycle/批大小、同 seed；
  **唯一差别 = `timestamp_target` = absolute vs duration**；
- 趋势都记 §6 的两条数（起始点中位误差、`spearman_duration`）＋ §5 的主指标；
- 判定不再只看终点，而是看**两条轨迹是否分离**：
  若 duration 臂的秩相关上升到 >0.5 而误差仍不优于绝对臂 ⇒ 参数化确实切换成功但**无收益**，
  这才是"结构参数化无效"的合格否证；
- 若两臂起始点误差都退化（§6e 情形 3）⇒ 是 LR 太猛，属实验缺陷，需降 LR 重做而不是下结论。

顺便记录一个我一度想走的省事方案，**它不可信、已自我否决**：
用 `uniform-6000`（同一存档更早的中间点）当"6000 步对照"。
它训练了 12000 步、只是在 6000 步处存档，**其优化历史与"只训 6000 步"完全不同**，
拿来当等算力对照会把"多训"的影响算错方向。这条写下来是为了防止我以后偷懒复用同一个想法。

### 6g. 【17:59 实测登记】step-1000 出现**起始点头崩塌**，并给出带截止时间的停止判据
逐字符核对（臂 step1000 的 60 条 / 485 字符，与基线同一批条目的交集）：

| 指标 | 起点 uniform-12000 | 臂 step1000 |
|---|---|---|
| 起始点误差 >0.2 s 的字符占比 | **0.7%** | **13.8%** |
| 起始点 p90 | 60 ms | **1335 ms** |
| 配对Δ（臂−起点） | — | 中位 +0.0 ms、**均值 +359.8 ms**；更差 99 / 更好 17 |

坏点**按歌集中**（如 `Soprano-3#飞云之下` 11 字符坏 7、`Alto-5#至少还有你` 15 坏 9、`Alto-6#至少还有你` 22 坏 8）
⇒ 条目级崩塌而非零散噪声。
同时第二槽位在"量级"上确实在收敛（估时长中位 1.12 s → 0.56 s，真值 0.39 s），
但**秩相关仍为 0（−0.018）**⇒ 只学到幅度、没学到顺序。

**判读规则（登记于看到 step-2000 之前）**：
- duration 参数化下 `end = start + duration` ⇒ 起始点误差是结束点误差的**硬下界**；
  因此**停止判据**：到 **step 2000** 若"起始点误差 >0.2 s 的字符占比"仍 > 5%
  ⇒ **立即停止本臂**，判定为 §6e 情形 3（本臂无效，不是参数化被否证），
  省下的 GPU 时间归还；不追加"再等等看"。
- 若在 step 2000 前起始点占比已回落到 ~1% 量级 ⇒ 崩塌是过渡期现象，臂继续，
  判决仍按 §5 + §6e 走。
- 无论哪种，**报告都必须把"完整 LR 热启动砸坏起始点头"作为本实验的设计缺陷候选写出来**；
  下一次设计应把 LR 减半或冻住起始点相关的投影更新。

这条登记的目的：防止我在 23:40 之后把"臂无效"讲成"参数化无效"。

## 8. 备用臂（LR 减半）与启用条件（18:01 登记）
`configs/training/qwen_fa_lora_duration_target_lowlr_20260914.yaml` 已备好，
**与在跑臂唯一差别 = projector_lr 2e-5→1e-5、lora_lr 5e-5→2.5e-5**
（其余逐项一致：目标 duration、起点 uniform-12000、批大小/梯度累积/cycle 3000/seed/max_steps 6000）。

启用条件（写死，防临场找理由）：
- **只有当 §6g 的停止判据在 step 2000 真的触发**（起始点 >0.2 s 占比仍 >5%、臂被停）
  ⇒ 才用这份配置重跑；预计 19:00 判定，若触发则新臂约 19:05 上卡、02:30 出结果；
- 若 step 2000 起始点已回落 ⇒ **本配置不使用**，在跑臂继续，不做"再补一条臂"的加练；
- 任何时刻**不同时跑两条臂**（单卡串行）；磁盘门禁 ≥15 G 照旧。

为什么这不是"再调一次超参碰运气"：§6g 的崩塌是**已观测到的实验缺陷**（起始点头被砸坏），
而 duration 参数化下起始点误差是结束点误差的硬下界 ⇒ 不修它就无法回答参数化的问题。
修的是让实验能回答问题的那个变量，不是想让结果变好的那个变量。

### 6h. 崩塌的**性质**：不是自信跑偏，而是判别性被打散（18:02，零算力诊断）
把 step-1000 的字符按"起始点是否 >0.2 s"分组，看它们**自己那一槽**的不确定性：

| 分组 | n | 熵(nats) | p_top1 | 真值排名中位 | 排名=1 占比 |
|---|---|---|---|---|---|
| 起始点未坏（臂） | 418 | 1.77 | 0.502 | 1 | 75% |
| 起始点已坏（臂） | 67 | **2.79** | **0.203** | **6** | 6% |
| 同一批 67 字符（基线 uniform-12000） | 67 | 0.75 | 0.691 | 1 | 75% |

⇒ 这些字符在基线上是**容易样本**，在臂里变成高熵低置信 ⇒ **判别性被打散**，不是"错得自信"。
⇒ 与"完整 LR 下第二槽位换语义产生的梯度冲击共享特征"一致；
   **排除标签变换 bug**：`duration_target_class_ids` 不改起始槽位（有单测固定）。
⇒ 结论：备用臂降 LR 是针对成因的修法（§8），而不是"碰运气调参"。

### 6i. 由此得到一条可复用的监控规则
**改标签语义做热启动时，监控"没被改动的那个槽位"的熵 / p_top1。**
它比终态指标灵敏得多：几百步内就能看到共享特征被连带冲击，
而不必等 6000 步跑完才发现"起始点头已经被砸坏"。
本轮起这是结构类实验的默认探针之一（趋势工具已经在记起始点误差，下一步可把熵一并记）。

## 9. 等算力对照臂（19:15 启动；设计写死在看到它的任何结果之前）
停臂后 GPU 空闲。原计划的「LR 减半重跑」被我**自己否决**：半速 6000 步比全速更不可能完成语义切换，
在 step 2000 全速都只有 ρ≈0.02 的情况下，它的期望信息量很低。

改为跑**唯一差别是问法**的对照组：
- 配置 `configs/training/qwen_fa_lora_absolute_control_6000_20260914.yaml`
  = 与已停臂逐字段相同，**只把 `timestamp_target` 从 duration 改回 absolute**
  （已用脚本逐字段核对，输出 `与已停臂的唯一差别: {"timestamp_target": [...]}`）；
- 同起点 `uniform-12000`、同步数 6000、同完整 LR、同 cycle 3000、同 seed、同 save 500；
- 于是「多训 6000 步的影响」和「完整 LR 对起始点头的连带损伤」**在两边同等发生**，
  同步数比较才能把"问法本身"的差别隔离出来。

**主比较（同步数配对，不做选点）**：在 step 500 / 1000 / 2000（时长臂已有这三个存档）上，
用同一批验证条目、同一导出工具，比较两臂的**长字符（≥1s）结束点误差**与**时长秩相关**。
- 若对照臂在这些点上保持基线水平、而时长臂显著更差 ⇒ **换问法本身有害**（干净的否证）；
- 若两边一样差 ⇒ 差异来自"热启动继续训练 + 完整 LR"这个共同因素，**参数化未被否证**，
  只是本预算下无法与共同损伤分离；
- 若时长臂反而更好 ⇒ 才有理由继续投这条线（并补 §7 的更长预算设计）。

停止/预算：对照臂跑到 6000 步或被同样预注册的健康判据停止；
它的终态验证与漏斗被 `save/eval` 设置挡住（absolute 模式下本可用，但我保持与另一臂同配置以免引入第二个差别）。

## 10. 同步数对比的第一个结果（19:52 自动产出，step 500 vs step 500）
**对照臂（绝对语义、多训 500 步）与起点几乎无差别**：开始时间中位 25 ms（起点 25 ms）、
p90 55 ms（起点 60 ms）、熵 0.61 / p_top1 0.793（起点 0.61 / 0.791）、
结束点超差率 2.47%（起点 1.92%）、**时长秩相关 0.925（起点 0.924）**。

**⇒ 关键推论：§6h/§6g 观察到的"开始时间点被砸坏"不是"完整 LR + 热启动续训"的共同代价，
 而是"换问法"这件事本身造成的。** 这条排除很有价值——它把混杂从我先前担心的方向上摘掉了。

**同算力配对（同一批 485 字；old=对照臂，new=时长臂，正数=时长臂更差）**：
| 位置 | n | 均值Δ(ms) | 截尾均值 | McNemar 好/差 | 校正后 p | 判定 |
|---|---|---|---|---|---|---|
| 结束点·短字 | 417 | +1595.6 | +1526.1 | 2 / 324 | 0.000 | **确证** |
| 结束点·长字 | 68 | +2057.9 | +2056.5 | 0 / 58 | 0.000 | **确证** |
| 开始点·短字 | 417 | +311.3 | +237.1 | 1 / 52 | 0.000 | **确证** |
| 开始点·长字 | 68 | +428.2 | +362.1 | 0 / 13 | 0.001 | **确证** |

⇒ 命中 §9 预注册的**第一分支**：对照保持基线、时长臂显著更差 ⇒ **这是对"换问法"的一次干净否证**。
⇒ 仍要写清的边界：(a) 这只到 step 500（时长臂的过渡期里最不利的一刻之一），
   但它在 1000 / 2000 步也没追回（结束点中位 860 / 830 ms 对 对照 ~25 ms，秩相关 −0.02 / +0.02 对 0.925）；
   (b) 因此"再多给预算会不会反转"仍属未证，但**代价已经量化**：想验证换问法能不能学会，
   要先付出上千步的全面退化，而起点本身没有义务付这个代价；
   (c) 综合 B（改采样）、C（改权重）、时长参数化三条：**在不改网络结构的前提下，
   所有"重新分配监督"的做法都不划算**——这比"某个想法失败"更有用。

### 10b. 对照臂的收尾安排（19:58）
判决已在 step 500 命中预注册第一分支 ⇒ 对照臂**没有理由跑到 6000 步**。
安排：让它跑到 2000 步凑齐 §9 计划的三个同步数对比点（500/1000/2000），
然后由 `stop_control_after_verdict.sh` **计划内停止**（写独立标记
`absolute_control.stopped_after_verdict`，**不是** `.done.failed`，避免以后被误读成故障）。
省下的显卡时间转去做成品验证（更贴近交付质量）：
B4 批的 `r2/vocal/full`（整首不分窗）形态成品零时长高达 **25.4%**，
那是比 33 首 KTV 批更苛刻的条件 —— 用整句一起挑在那上面能不能把坍缩清零，
才是这条改进最硬的一次考验。

### 10c. 第二个同步数对比点（step 1000，20:27 自动产出）——完全复现第一点
同一批 485 个留出字符：

| 谁 | 开始时间 p90 | 开始时间熵 | 结束点超差率 |
|---|---|---|---|
| 起点 uniform-12000 | 60 ms | 0.61 | 1.92% |
| 对照臂 @1000（只多训，不问法改变） | **55 ms** | **0.59** | **2.06%** |
| 时长臂 @1000（换了问法） | 1335 ms | 1.93 | **73.40%** |

配对（正数 = 换问法更差），四项全部过 ×4 校正：
| 位置 | n | 均值Δ | McNemar 好/差 | 校正后 p | 判定 |
|---|---|---|---|---|---|
| 结束点·长字 | 68 | **+1888.8 ms** | 1 / 62 | 0.000 | **确证** |
| 结束点·短字 | 417 | +1398.9 ms | 2 / 287 | 0.000 | **确证** |
| 开始点·短字 | 417 | +358.4 ms | 1 / 58 | 0.000 | **确证** |
| 开始点·长字 | 68 | +368.4 ms | 0 / 9 | 0.016 | **确证** |

⇒ 500 步与 1000 步两个独立同步数点给出同一形状 ⇒ §9 第一分支成立：
  **「起始+时长」参数化在等算力下被干净否证，且对照臂证明这不是"多训"或"LR"的代价。**
⇒ 不因"结论已清楚"而提前停止对照臂：仍按计划跑到 step 2000（时长臂正是死在那一步，
   那个点的对比最有说服力），到点即按计划内停止释放显卡（§10b）。
