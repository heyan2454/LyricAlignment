__pycache__/
*.pyc
.venv/
.env

# Python/build/test caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# Local path and asset configs
local_paths.yaml
configs/paths/local_paths.yaml
configs/assets/assets.local.yaml
configs/assets/smoke_samples.local.yaml

# Local ML/data roots and outputs
/external_data/
/datasets/
/models/
/checkpoints/
/outputs/
/wandb/
/.cache/

# Run-local large outputs
gt_eval_out/
out/
*.bak.json

# runs 目录只登记轻量 README/.gitkeep（参考 AST 模式），具体 run 数据放
# /home/hyan/Data/lyricalign/runs/，禁止进入工作目录与 git。
runs/**/*
!runs/**/
!runs/**/.gitkeep
!runs/README.md

# reports 生成资产不进 git（AST 模式）；md/json 源文件保留。
reports/**/*.pdf
reports/**/*.pptx
reports/**/*.docx

# Audio/video and model artifacts
*.wav
*.mp3
*.mp4
*.m4a
*.flac
*.ckpt
*.safetensors
*.pt
*.pth

# Standalone local demo assets and generated videos
/夜苏打/

# Generated only inside packaged ZIP files; never track as repository source.
/ARCHIVE_MANIFEST.generated.json

# 大型逐条产物外置于数据目录，results/ 只留轻量指标（AGENTS.md 约定）
results/**/per_character.jsonl
results/**/baseline_drift.json
results/**/baseline_aligned.json
results/**/long60_baseline.json

# 大型逐字符分布导出（7 MB 级）：留在磁盘、不入 git；只入 summary/metrics JSON
results/**/rows.jsonl
