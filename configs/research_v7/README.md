# research_v7 配置说明

本目录为下一阶段实现预留配置规范。当前 patch 不含正式实验实现。

- `mutation_catalog.example.yaml`：百分比 mutation、donor/no-match 和 workflow 枚举；
- formal 前应生成冻结的 `mutation_catalog.yaml`、`pilot_freeze.json` 和 donor manifest。
