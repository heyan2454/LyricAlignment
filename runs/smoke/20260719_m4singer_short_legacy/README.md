# Legacy M4Singer Short Smoke

该目录是对 2026-07-19 已执行 smoke 的轻量历史摘要，不是重新运行产生的完整证据。

原始逐样本 JSON 位于服务器外部路径，但没有随旧包归档，也没有记录 SHA256、命令和配置 hash。因此本目录只保存可确认的报告事实，并明确证据限制。下一次服务器运行必须使用 schema v2 的标准 smoke 脚本自动生成证据。
