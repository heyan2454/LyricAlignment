# Real-GT Expansion Handoff Session Record

**Session:** `research_transition_recovery_detector_20260810_realgt_expansion_handoff`  
**State:** `prepared_for_opencode`; no inference, training, recovery, or metric run has started here.  
**Purpose:** isolate real-GT expansion and all GT-contaminated reruns from the archived 2026-08-07--10 work.

## Immutable evidence sources

Read, but do not write new artifacts into, the preceding work and planning directories:

- `docs/research_transition_recovery_detector_20260808_correction/14_...md` through `19_...md`;
- `runs/research_transition_recovery_detector_20260807/`;
- `/root/autodl-tmp/lyricalign_sessions/20260808_corrected/` and `/root/autodl-tmp/lyricalign_sessions/20260809_signal_completion/`;
- M4Singer overlay `/home/hyan/Data/lyricalign/derived/20260723_m4singer_overlay_slur_time_v1/`;
- R2 split `/home/hyan/Data/lyricalign/derived/20260723_qwen_fa_lora_v1/split/m4singer_accepted_split_manifest.jsonl`.

## Session contents

- `metadata/plans/OPENCODE_EXECUTION_BRIEF.md`: mandatory ordering and safety gates.
- `metadata/plans/GT_CONTAMINATED_RERUN_MATRIX.md`: affected experiments and rerun/reaggregation requirements.
- `metadata/plans/source_documents/20_PRETRANSITION_GT_LINEAGE_AUDIT_AND_RERUN_PLAN_20260810.md`: mandatory pre-Transition lineage audit and generated rerun-manifest requirements.
- `metadata/lineage/`: frozen cohort, GT, cache, and historical lineage tables generated by OpenCode.
- `artifacts/`: new outputs only. Existing reports must never be overwritten.

## Immediate stop conditions

Do not begin GPU work if a cohort overlaps R2 train, GT is not accepted-overlay timing on the global clock, an unlabeled unit receives a correctness label, or cohort/split/input hashes have not been frozen.

## 2026-08-11 OpenCode 自由探索追加（CPU 分析 + 一次性 GPU 子窗实验）

- **E1（CPU）**：R-combo detector（fit B train → score A test，全 OOS）window 级质检门 AUROC 0.977-0.983；mean_pb>0.3 捕获全部 15 个 bad window，好窗误伤 16%。结论：行级 p_bad 聚合可在 GT 不可得时作 formal 前质检。
- **E2（CPU）**：LOO B-song 冻结敏感性 → 所有 LOO 点 constraint_violated=true；val=我爱你中国(115 unsafe) 时冻结坍缩 reject-all（T_accept=0）。现行「真实」(29 unsafe) 是唯一 A test pr/sa 平衡锚点但脆弱。详见 `metadata/reports/EXPLORATION_NOTES.md` §5。
- **绒花:w2 细分窗 GPU 实验**：把灾难窗按语义切成 4×≤20s 子窗重跑同一模型 → 彻底消除窗口起点塌陷，max_mae>15s→4.4s，合并 mean_mae=1.44s、hit100=0.40（原 60s 整窗 hit100=0）。**窗口规划细分（≤20s/重复段拆开）是可行修复，无需换模型**。详见 EXPLORATION_NOTES §6。

产物：`metadata/reports/EXPLORATION_NOTES.md`、`/tmp/opencode/rerun_w2_subwindow.jsonl`、`/tmp/opencode/w2_subwindow_run/`、`/tmp/opencode/eval_subwindow.py`、`/tmp/opencode/explore_detector_sensitivity.py`（+ `.md` 明细）。

- **detector 特征增强（CPU 消融，证伪）**：R+repair/crossview/neigh/all 五种特征集 LOO B-song 全部 0/5 constraint_satisfied（我爱你中国 unsafe=115 恒致 T_accept=0 reject-all）。冻结坍缩是结构性机制问题，非特征不足。详见 EXPLORATION_NOTES §8。

- **【P0】light_merge island-fill 破坏保护优先召回**：孤立单点 REJECT 夹 ACCEPT 被翻转成 ACCEPT，保护优先点 raw pr=1.000 → merged pr 0.655-0.833（全世界失眠/真实/阴天快乐/已是两条路上的人）。修复模拟（只填 ACCEPT 岛）后 pr 全=1.000，LOO 约束满足 0/5→2/5。详见 EXPLORATION_NOTES §9。
- **【P1】window 级质检门槛全量落地**：90 请求扫描，推荐 frac>T_accept>0.45（AUROC 0.9831，15/15 bad 全捕获，FP 12%）；E1 的 mean_pb>0.3 在 90 请求下漏 3。详见 §10。
- **【P1】语义窗口规则 v2 形式化**：重复 run 相交判定 + GAP_RUN_SPLIT，90 请求回测：含 run 的 bad（绒花 w1/w2 系列）全可拆；月满西楼:w1 坏因非「啊」段，待单独归因。详见 §11。
- **【P1】语义窗口规则 GPU 验证通过**：规则 v2 自动切窗（绒花 w1/w2 各 4 窗）真实 forward，合并 hit100=0.53 ≈ 人工切分 0.57（mean_mae 1.21 vs 1.08），max 4.44s 消除窗口起点塌陷（整窗基线 40.6s）。规则可作 formal window-plan 自动切窗策略。详见 EXPLORATION_NOTES §12。
- **【P2】月满西楼:w1 坏因归因**：句首衬词序列「头哦无哦偶」时间戳错位（偶 +7.29s），非重复 run；规则不可拆，建议 repair 或 detector 识别句首衬词模式。详见 §13。

- **【落地】light_merge P0 修复**：detector_v2_intervals.py island-fill 改为只填 ACCEPT 岛（REJECT 岛不再翻转），新增 3 个回归测试（单点/级联 REJECT 岛不翻转、tristate 保 REJECT）。commit cf20bb6；全模块 tests/research_v7 506 passed 无回归。
- **【落地】语义窗口规则 v2 模块化**：src/lyricalign/research_v7/semantic_window_planning.py（detect_repeat_runs / window_contains_run / clip_runs_to_window / plan_window / plan_semantic_windows）+ 契约测试 16 个。commit ee72ba1。
- **【review】2 并行 review**：代码正确性+契约 → 未发现 P0/P1（1 MINOR docstring 措辞已修）；数据一致性+接线 → P1 字段契约已修（to_dict 对齐 requests.py canonical_text_start/end 不含端点，33f4e22）。全模块 506 passed。

- **【真实数据验证】light_merge 修复在真实 evidence 上的效果**（stage3b_cohort_ab_eval）：用修复后代码重跑 evaluate_detector_v2（同一冻结操作点 FROZEN_OPERATING_POINTS.json）。
  - FAMILY_LOO：official pr 0.9216→0.9706（baseline）/0.9103→0.9517（missing）；raw pr 0.9086→0.9624/0.8905→0.9489——pr 全面恢复 +0.05~0.06。
  - sa 同步下降 0.06~0.12（official 0.8566→0.7478，raw 0.9062→0.8421），但冻结约束 min_safe_accept_rate=0.2，sa=0.72-0.84 远未越界。
  - 冻结操作点本身 constraint_violated=true（pr=0.655，坍缩态）——修复让同一冻结阈值下 pr 恢复到 0.95+，pr/sa 权衡在约束内。
  - 结论：P0 修复在真实数据上确认非退化且恢复保护召回。产物：/tmp/opencode/lm_verify/M4_SONG_HELDOUT_FIXED.json/（FAMILY_LOO.json、M4_SONG_HELDOUT.json）。

- **【落地】语义窗口规则接入 window-plan 管线**：build_requests() 加 use_semantic_windows=False 开关（默认 fixed-60s 冻结口径不变）+ --window-mode CLI + FREEZE.json 记 window_mode；semantic 模式用 plan_request_windows 把每个 fixed 候选窗切成子窗（音频区间=子窗首末 unit 时间，canonical 连续不相交），请求带 :s{sub_idx} 后缀。commit 6d6b0db。
- **【落地】接线 review P1 修复**：slot_plan_id/comparison_group_id 由 plan_group 派生，初始未带子窗后缀 → 同一 fixed 窗切出的子窗共享同一 group。修复 plan_group 并入 sw_sfx（pair_id==comparison_group_id 恢复），补断言测试。commit 7e39d5e。复查确认 fixed 口径逐字节不变、missing 继承属既有冻结口径、phase 名唯一。
- 接线全绿：tests/research_v7 511 passed。待办：semantic 模式真实 GPU 跑一次（vs fixed 基线），验证 real 执行与评价链路无缺口。
