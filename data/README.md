# Data

本目录只保存轻量 manifest、schema、数据登记和小型统计，不保存真实大音频、模型权重或外部下载包。

- `manifests/`：raw、curated 和 split 索引模板。
- `schemas/`：annotation、prediction 和 curation record schema。
- `datasets_registry.md`：候选数据集、许可、粒度、状态和使用边界。

外部数据布局与清洗协议见 `docs/manual/data_preparation.md` 和 `docs/manual/data_curation.md`。
