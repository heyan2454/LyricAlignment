# 去掉塌陷单元后，长音收尾的跨域反向偏置还在吗（第 51 轮，2026-09-13）

> 数字由 `runs/20260912_bias_excluding_degenerate/BIAS_EXCLUDING_DEGENERATE.json` 生成；只读既有面板，零前向、零 GPU。偏置＝预测减真值（负＝切早，正＝切晚）。

## 0. 结论

- **反向偏置成立，而且更强**：录音室（GTSinger 全检查点，第 21 轮同一口径）长音收尾中位 **-40.0ms → -48.0ms**（偏晚比例 23.9% → 22.1%）；伴奏域（MIR-1K）长音 **32.4ms → 34.0ms**（74.1% → 74.8%）。**一负一正，符号相反 ⇒ 全局偏移校正不可跨域迁移这一条结论继续成立**；
- **塌陷单元本身在反向**：录音室被压平的字长音中位 **+10.0ms**、偏晚比例 75.0%（把它们混进来会**掩盖**真实的提前偏置）⇒ 去掉之后提前量反而变大；
- r2 单检查点同样成立（-14.5ms → -16.0ms，偏晚比例 33.8% → 32.0%）。

## 1. 明细

### GTSinger 全检查点（第 21 轮口径）

| 子集 | 单元 | 全部长字中位 | 偏晚比例 |
|---|---:|---:|---:|
| 含塌陷 | 30,600 | -40.0ms | 23.9% |
| 去掉塌陷 | 29,240 | -48.0ms | 22.1% |
| 只看塌陷 | 1,360 | 10.0ms | 75.0% |

### GTSinger 仅 r2

| 子集 | 单元 | 全部长字中位 | 偏晚比例 |
|---|---:|---:|---:|
| 含塌陷 | 10,200 | -14.5ms | 33.8% |
| 去掉塌陷 | 9,840 | -16.0ms | 32.0% |
| 只看塌陷 | 360 | 30.0ms | 100.0% |

### MIR-1K r2_full

| 子集 | 单元 | 全部长字中位 | 偏晚比例 |
|---|---:|---:|---:|
| 含塌陷 | 2,035 | 32.4ms | 74.1% |
| 去掉塌陷 | 2,023 | 34.0ms | 74.8% |
| 只看塌陷 | 12 | -100.1ms | 0.0% |

## 2. 这条结论现在有了更干净的口径

- 第 21 轮报的「录音室提前 40ms / 伴奏延后 32.4ms」是**含塌陷**的混合值；今后引用应改用**去掉塌陷**的版本（录音室 −48ms、伴奏 +34ms），或在同一处并列两者；
- 判定不变（符号相反 ⇒ 不做全局偏移校正），但**依据更硬**：塌陷不但没有造成这个反向，反而在往回掩盖它。

## 3. 复现

```bash
source /root/miniconda3/etc/profile.d/conda.sh && conda activate lyricalign-qwen
cd /home/hyan/LyricAlignment
PYTHONPATH=src python scripts/evaluation/run_bias_excluding_degenerate.py
PYTHONPATH=src python scripts/evaluation/report_bias_excluding_degenerate.py
```

