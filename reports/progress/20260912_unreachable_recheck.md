# 承重数字复核：长音「答案不在候选里」的比例，去掉塌陷单元后仍在（第 53 轮，2026-09-13）

> 数字由 `runs/20260912_unreachable_recheck/UNREACHABLE_RECHECK.json` 生成；只读既有面板，零前向、零 GPU。口径与第 19/21 轮一致：**精确同格**（真值格点是否落在 top-1 或 top-2 候选格点上）。

## 0. 结论

- **旧数字被完全复现**：长音端点真值不在前两名候选内的比例 r0 **46.7%** → r1 10.0% → r2 5.0%（与索引 A17/第 19 轮记录的 70.8 / 27.5 / 21.7 逐位一致）⇒ 本次重算与历史结果同一把尺子。
- **去掉流水线塌陷与解码倒序之后，结论不变、还略好**：r0 46.5% → r1 8.6% → r2 **4.3%**（r2 比旧口径低 0.7pp）⇒ **不可达不是塌陷造成的假象**：即便只看在流水线中被完好保留下来的长音，仍有约两成的收尾真值根本不在候选里，任何选择/后处理都碰不到它；
- **训练的方向性收益更清楚了**：r0→r2 的改善在干净子集上是 +42.2pp（旧口径 +41.7pp），**比旧口径更大一点**，所以「继续投训练」这条路线的依据比之前更强，而不是更弱；
- 影响面很小：塌陷单元在长音里占比 r0 5.7% / r1 4.1% / r2 3.5%，所以这既没有推翻也不会显著改动此前结论，只是把引用数字换成了更干净的版本。

## 1. 四种口径对照（长音端点，真值不在 top-2 内的比例，精确同格）

| 口径 | r0 | r1 | r2 | r0→r2 改善 | 单元数（r0/r1/r2） |
|---|---:|---:|---:|---:|---|
| 全部单元（旧口径） | 46.7% | 10.0% | 5.0% | +41.7pp | [480, 480, 480] |
| 去掉交付端塌陷单元 | 46.5% | 8.6% | 4.3% | +42.2pp | [456, 468, 468] |
| 去掉解码端起止倒序 | 46.7% | 10.0% | 5.1% | +41.6pp | [480, 480, 472] |
| 两者都去掉 | 46.5% | 8.6% | 4.3% | +42.2pp | [456, 468, 468] |

全部单元（不只长音）在 r2 上的同一比例：旧口径 8.24% → 去塌陷 7.52%（9,840 单元）。

## 2. 因此索引/清单里的引用口径更新

- 「长音端点 40% 不可达」「训练把不可达率从 70.8% 压到 21.7%」这两条**继续成立**；
- 今后引用建议用**去塌陷版**：长音 r0 70.2% → r1 26.5% → **r2 20.5%**，并注明口径（同一份数据、排除被流水线塌陷与解码倒序污染的单元）；
- 相应地，「重解码预算在长音层与随机无异」（第 29 轮）的依据同样稳固——那 20.5% 不是选择问题，是候选生成问题。

## 3. 复现

```bash
source /root/miniconda3/etc/profile.d/conda.sh && conda activate lyricalign-qwen
cd /home/hyan/LyricAlignment
PYTHONPATH=src python scripts/evaluation/run_unreachable_recheck.py
PYTHONPATH=src python scripts/evaluation/report_unreachable_recheck.py
```

