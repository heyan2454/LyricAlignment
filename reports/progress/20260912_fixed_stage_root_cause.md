# 零长度塌陷的根因：上游解码器的「单调化修补」把整块填成同一时刻（第 45 轮，2026-09-13）

> 数字由 `runs/20260912_fixed_stage_root_cause/FIXED_STAGE_ROOT_CAUSE.json` 生成；零前向、零 GPU。第 44 轮已把放大归因到 `fixed` 阶段，本轮把该阶段**落到具体函数**并做可复现验证。

## 0. 一句话根因

- 交付里的 `fixed_*` 时间戳**原样抄自上游处理器**：`transformers.models.qwen3_asr.processing_qwen3_asr._fix_timestamps`（由 `Qwen3ASRProcessor.decode_forced_alignment` 调用；transformers 5.15.0.dev0；上游出处见其 docstring：QwenLM/Qwen3-ASR qwen_asr/inference/qwen3_forced_aligner.py L147 (per docstring)）。
- 该函数把「不属于最长递增子序列」的整块时间戳修补掉，而**当这一块触到序列两端（或两侧好值相等）时，它用同一个常数填满整块** ⇒ 块内所有时间戳相同 ⇒ 对应字的时长为零：
  `[0, 50, 40, 30, 20, 10, 0] → [0, 50, 50, 50, 50, 50, 50]`（不同取值 6 → 2）。
- 边界条件对照：两侧都有不同好值时它会**线性插值**、不会塌陷（`bounded_block_stays_distinct = True`）⇒ 塌陷专属于「邻居缺失/相等」这条分支。

## 1. 真实数据复现（把交付值离线重算出来）

做法：对每首歌按窗口取出记录里的原始时间戳（相邻两个槽为 start/end，单位毫秒），喂给上游同一个函数，与交付的 `fixed_local_*` 比对。

| 歌 | 单元 | 槽位吻合率 | 零长（原始→交付） | 倍数 | 最差窗口 |
|---|---:|---:|---|---:|---|
| 初音未来的消失 | 561 | **97.6%** | 266 → 429 | ×1.613 | 窗口 2（221 字）：零长 113 → **221**，该窗口预测值只剩 **11** 个不同取值 |
| I See Fire | 311 | **96.5%** | 160 → 274 | ×1.712 | 窗口 2（199 字）：零长 107 → **199**，该窗口预测值只剩 **1** 个不同取值 |
| 画下灯塔水母 | 532 | **99.8%** | 112 → 205 | ×1.83 | 窗口 3（148 字）：零长 83 → **135**，该窗口预测值只剩 **18** 个不同取值 |
| 冬之花 | 222 | **91.7%** | 98 → 139 | ×1.418 | 窗口 1（112 字）：零长 66 → **112**，该窗口预测值只剩 **1** 个不同取值 |
| Camelia | 334 | **99.7%** | 57 → 66 | ×1.158 | 窗口 0（76 字）：零长 17 → **22**，该窗口预测值只剩 **84** 个不同取值 |
| Immortals | 200 | **100.0%** | 14 → 24 | ×1.714 | 窗口 1（77 字）：零长 5 → **9**，该窗口预测值只剩 **93** 个不同取值 |

- 汇总：6 首歌、槽位吻合率中位 **98.7%**；零长单元合计 **707 → 1137** 个。
- 最极端一例：**初音未来的消失** 的窗口 2，221 个字的窗口里原始有 113 个零长，交付后 **221** 个（整窗），而该窗口全部预测时间戳只剩 **11** 个不同取值（吻合率 94.1%）——即**整窗被压成一个时刻**。

## 2. 这如何修正前几轮的结论

- 第 43 轮「放大 11 倍」、第 44 轮「只在 fixed 阶段放大」都对，但根因**不在我们的后处理**，而在**上游 Qwen 处理器的单调化修补**：我们只是原样继承了它的输出；
- 第 15–17 轮的两条猜测需要区分：「倒序被静默钳成零长」（我们自己的 karaoke 预钳位）是真的一条，但它只解释一部分；**数量级更大的这条是上游常数填充**——本轮把两者区分开了；
- 原始阶段（我们自己取 argmax、不做修补）保留了真实跨度与更多不同取值 ⇒ **信息在修补前是存在的**，修补把它抹掉了。

## 3. 修法选项（供主线裁定，本会话不改上游代码）

1. **产品链路绕开该修补**：我们自己已有 `raw_global_*`（argmax，不塌陷），可改用它并配套一个**不产生零长度**的单调化（例如逐槽夹取到相邻好值、或按下限保证 `end > start`）；
2. **给上游打补丁**：把「邻居缺失/相等」分支从「填常数」改为「按相邻好值与块长分摊」或「保留原值并对两端做夹取」，可在此文件基础上做本地 patch 并加回归测试；
3. **兜底**：无论选哪条，交付前的结构自检（`audit_batch.py`）应把「同一时刻连续块 ≥ N」列为**告警**，避免整窗塌陷静默出厂。

## 4. 复现

```bash
source /root/miniconda3/etc/profile.d/conda.sh && conda activate lyricalign-qwen
cd /home/hyan/LyricAlignment
PYTHONPATH=src python scripts/evaluation/run_fixed_stage_root_cause.py --songs 6
PYTHONPATH=src python scripts/evaluation/report_fixed_stage_root_cause.py
PYTHONPATH=src python -m pytest -q tests/evaluation/test_fix_timestamps_collapse.py
```

