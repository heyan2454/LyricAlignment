{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "general": {
      "steps": 8,
      "description": "General-purpose subagent for research/implementation tasks. Steps capped at 8: at the last step the agent must stop and summarize state (done/blocked/artifacts/next) instead of looping."
    },
    "explore": {
      "steps": 8,
      "description": "Fast codebase exploration subagent. Steps capped at 8: at the last step the agent must stop and summarize findings instead of looping."
    }
  },
  "provider": {
    "opencode-go": {
      "options": {
        "timeout": 300000,
        "chunkTimeout": 60000,
        "headerTimeout": 15000
      }
    }
  }
}